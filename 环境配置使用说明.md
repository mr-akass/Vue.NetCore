# 环境配置使用说明

## 配置文件结构

项目支持多环境配置（vol.api.sqlsugar/VOL.WebApi 下）：

- `appsettings.json` - 基础配置（所有环境通用，含兜底的数据库连接）
- `appsettings.Development.json` - 开发环境专用配置（RunningEnvironment=DEV）
- `appsettings.Staging.json` - 测试环境专用配置（RunningEnvironment=STG）
- `appsettings.Production.json` - 生产环境专用配置（RunningEnvironment=PRD）

环境专用文件中一般只放：数据库连接（Connection）、Redis、CorsUrls、RunningEnvironment 标识、日志级别等每个环境不同的配置。

## 配置合并规则

1. 首先加载 `appsettings.json` 基础配置
2. 然后根据 `ASPNETCORE_ENVIRONMENT` 环境变量加载对应的环境配置文件，**覆盖**基础配置中的同名配置项
3. 环境配置文件中没有的配置项继续使用基础配置

## 环境切换方式

### 方法1：根目录 run.bat（推荐）

双击仓库根目录的 `run.bat`，按提示选择环境（1=Development / 2=Staging / 3=Production），会自动分窗口启动前端（npm run dev）和后端（dotnet watch，带环境变量）。

### 方法2：Visual Studio 启动配置

`Properties/launchSettings.json` 已配置好按环境的启动 profile：

- `VOL.WebApi.Development`
- `VOL.WebApi.Staging`
- `VOL.WebApi.Production`

在 VS 启动按钮的下拉里选择对应 profile 即可。

### 方法3：命令行

```bash
# 设置环境变量后启动
set ASPNETCORE_ENVIRONMENT=Staging
dotnet run

# 或者直接指定 launch profile
dotnet run --launch-profile VOL.WebApi.Staging

# 或者命令行参数
dotnet run --environment=Production
```

也可以用 `VOL.WebApi` 目录下的脚本：

```bash
start_api_watch.bat Staging   # watch 模式
start_api.bat Production      # 普通模式
```

## 验证当前环境

后端启动时控制台会输出：

```
==================== 环境配置 ====================
ASPNETCORE_ENVIRONMENT: Staging
RunningEnvironment:     STG
配置文件: appsettings.json + appsettings.Staging.json
==================================================
```

代码中也可以随时读取：

- `AppSetting.RunningEnvironment` — 环境配置文件里的 DEV/STG/PRD 标识
- `app.Environment.IsDevelopment() / IsStaging() / IsProduction()` — ASP.NET Core 标准判断

## 注意事项

1. **发布部署时**必须给站点/服务设置 `ASPNETCORE_ENVIRONMENT` 环境变量（IIS 在配置编辑器 aspNetCore/environmentVariables 中设置；Windows 服务用 setx 或服务配置），否则默认按 Production 加载
2. 基础 `appsettings.json` 中保留了一份数据库连接作为兜底，正式使用后建议把各环境连接改为对应环境的真实值
3. 新增配置项时：所有环境相同的放 `appsettings.json`，因环境而异的放对应环境文件
