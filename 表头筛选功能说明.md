# 表头筛选功能说明

## 功能概述

表头列筛选为**框架级全局功能**，所有使用 `view-grid`/`VolTable` 的页面均可使用。筛选交互会**根据列类型自动适配**：

| 列类型 | 选项来源 | 输入框行为 | 查询方式 |
|---|---|---|---|
| 普通列（文本/数字等） | 后端分批加载去重值（每批 30 条） | 关键字模糊查询（LIKE） | 输入框 like / 勾选 in |
| 字典列（配置了 `bind` 的下拉框、单选等） | **直接使用绑定的数据字典**，选项显示字典文本（如 男/女） | 客户端过滤选项列表，不参与查询 | 勾选的字典 key 以 in 查询 |
| 日期列（date/datetime） | 后端按**天**去重（yyyy-MM-dd），不再出现一堆精确到秒的时间戳 | 不显示输入框 | 勾选日期后按**当天区间**匹配（含当天所有时间） |

列上有生效筛选条件时漏斗图标显示为主题色。字典列若数据源尚未加载（如远程字典），自动回退到后端去重值方式。

## 如何启用

### 方式一：代码生成器（推荐）

代码生成器的列配置中已内置"**表头筛选**"勾选项（对应 `Sys_TableColumn.HeaderFilter` 字段）。
勾选后重新生成页面，`options.js` 的列配置会自动带上 `filterData:true`，无需写任何代码。

### 方式二：手动配置

在页面 `options.js` 的 `columns` 中给需要筛选的列添加 `filterData: true`：

```javascript
columns: [
  { field: 'UserName', title: '用户名', width: 120, filterData: true },
  { field: 'Gender', title: '性别', width: 80, bind: { key: 'gender', data: [] }, filterData: true },
]
```

## 用户操作流程

```
1. 点击表头列的筛选图标（漏斗图标）
   ↓
2. 弹出筛选弹窗（普通列自动加载前 30 条去重值；字典列直接显示字典选项；日期列显示按天去重的日期）
   ↓
3. 普通列：输入关键字模糊匹配，或勾选选项精确匹配（同时使用时输入框的值并入 IN）
   字典列：勾选字典选项精确匹配（输入框仅过滤选项列表）
   日期列：勾选日期，匹配当天所有时间的数据
   ↓
4. 需要更多选项时点击"加载更多"（每次 30 条，全部加载后按钮禁用；字典列无需加载）
   ↓
5. 点击"清空"移除该列筛选条件
```

## 技术实现

### 后端（框架级，vol.api.sqlsugar）

| 位置 | 内容 |
|---|---|
| `VOL.Core/Controllers/Basic/ApiBaseController.cs` | 通用接口 `POST api/{表名}/getColumnDistinctValues`，所有生成的控制器自动继承，权限校验同查询（Search） |
| `VOL.Core/BaseProvider/ServiceBase.cs` | `GetColumnDistinctValuesAsync`：列名经实体属性白名单校验（防 SQL 注入）后，用 SqlSugar 表达式 `GROUP BY` 去重分页；**DateTime 列按天分组**（`CAST(列 AS DATE)`）返回 yyyy-MM-dd；支持多数据库（MsSql/MySql/PgSql/Oracle/DM） |
| `VOL.Core/Extensions/LambdaExtensions.cs` | `in`/`notIn` 查询时，DateTime 列的值若均为 `yyyy-MM-dd` 格式（10 位），自动按**天区间**匹配（`>=当天 && <次日` 的 OR 组合）；带时间的值仍按精确相等匹配 |
| `VOL.Entity/DomainModels/Core/LoadSingleDataModel.cs` | 请求参数模型 `ColumnDistinctValueOptions { ColumnName, Page, PageSize }` |

去重查询与页面查询走同一套 `BuildPageDataQuery` 基础过滤（数据权限、多租户、逻辑删除），PageSize 上限 200。

服务层为 `virtual` 方法，单表需要特殊处理时可在该表 Service 的 Partial 类中重写。

### 筛选条件传递

应用筛选时前端把条件写入 `paginations.Filter` 数组，随 `getPageData` 一起提交：

```json
{ "Name": "UserName", "Value": "张三", "DisplayType": "like" }
{ "Name": "Name", "Value": "122,123", "Values": ["122", "123"], "DisplayType": "in" }
```

`in`/`notIn` 同时传 `Value`(逗号拼接，兼容老调用方) 和 `Values`(数组)，**后端优先用 `Values`**：
值本身含逗号时(如地区名 `北京市,新疆`)按 `Value` 拆逗号会把一个值拆成两个，都匹配不到。

后端 `ApplicationServiceBaseSearchExtensions.GetSearchParameters` 优先读取 `Filter` 数组，
`DisplayType` 经 `StringExtension.GetLinqCondition` 转为对应的 LINQ 条件。

**字符串列的 `in` 不生成 `IN (...)`，而是参数化的等值 or 链**
（`LambdaExtensions.GetStringInExpression`：`p=>p.Col==@p0||p.Col==@p1`）。
原因：SqlSugar 对集合 `Contains` 生成的是把值直接拼进 sql 的 `IN ('值')` 字面量，
而 SqlServer 对**不带 `N` 前缀**的字面量按数据库排序规则的代码页转换——
`SQL_Latin1_General_CP1_CI_AS` 这类非 Unicode 排序规则的库里中文会全部变成 `??`，
于是"英文数字筛得出、中文筛不出"。改成等值比较后 SqlSugar 会生成 `@参数`(nvarchar)，中文原样传库。
非字符串列(int/日期等)不受影响，仍走原来的 `IN`。

### 前端（vol.web）

- `VolTable.vue`：筛选弹窗 UI（el-popover + 输入框 + 多选框 + 加载更多）；
  `loadMoreFilterOptions` 将查询地址中的 `getPageData[Async]` 替换为 `getColumnDistinctValues` 请求选项；
  **字典列直接用 `column.bind.data` 作为选项**（显示字典文本，输入框做客户端选项过滤）；
  **日期列隐藏输入框**，选项为按天去重的日期
- `VolTable/VolTable.less`：弹窗样式
- `VolTable/VolTableLoadData.js`：查询参数附带 `Filter` 数组

## 注意事项

1. **字段名**：`ColumnName` 必须是实体属性名（不区分大小写），非实体属性直接返回错误
2. **旧版一次性实现已移除**：原 `RenController/RenService` 中的 `GetColumnDistinctValues`（原生 SQL 拼接、仅支持 Ren 表）已删除，统一走框架级接口
3. **vol.api（EF 版后端）未同步实现**：当前仅 vol.api.sqlsugar 支持
4. **组合筛选语义（仅普通列）**：输入框与多选框同时有值时，输入框的值并入 `IN` 列表做精确匹配（不再是模糊匹配）
5. **日期按天匹配的边界**：`in` 查询中带时间的值（如 `2026-07-09 00:00:01`）仍按精确时间匹配，只有纯日期格式（`2026-07-09`）才按天区间匹配
6. **中文筛选依赖参数化**：字符串列的 `in` 走等值 or 链（见上）。若以后有人把它改回集合 `Contains`，
   在非 Unicode 排序规则的库上中文会立刻失效（这不是编码配置问题，改库排序规则也不该是解决办法）
7. **值含逗号**：勾选值里带逗号时必须靠 `Values` 数组传，前端 `applyColumnFilter` 已同时传两者

---

**修改日期**：2026-08-24（中文筛选修复：字符串 in 改参数化 + 新增 Values 数组）
**实现方案**：框架级通用接口（基类控制器 + 基类服务）+ 代码生成器"表头筛选"勾选项 + 双重筛选 + 批量加载
