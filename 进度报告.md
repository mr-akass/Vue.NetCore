# 项目功能迁移进度报告

**更新时间**: 2026-08-24
**项目路径**: C:\Vue.NetCore
**参考项目**: C:\ShelfLifeMgt\ShelfLife.WebApi

---

## 一、需求明细

从 C:\ShelfLifeMgt\ShelfLife.WebApi 迁移以下功能到当前项目：

1. **多环境配置**: appsettings.Development/Production/Staging.json + run.bat ✅
2. **CustomConsole封装**: 控制台输出 + 日志记录 ✅
3. **多数据库连接支持**: 字典 + 代码生成器 + 前端都支持多数据库 ✅
4. **多角色支持**: 用户支持多角色 ✅
5. **SignalR功能**: 实时通信功能 ✅
6. **站内消息入库+已读未读**: SignalR消息持久化、消息中心 ✅ (第二批追加)
7. **定时任务设置频率生成cron**: Sys_QuartzOptions频率弹窗 ✅ (第二批追加)
8. **Sys_Application多应用/子系统**: 应用管理+角色关联+guide选择页+菜单按应用过滤+定制首页 ✅ (第三批追加)
9. **多语言(国际化)支持**: Sys_Language语言表+语言设置页面+生成语言包+前端切换语言 ✅ (第四批追加，参考 C:\vol.pro.ts_vite-master)
10. **登录页改版+去验证码** ✅ (第五批追加)
11. **代码生成器编辑模式(弹窗/新页面/行内编辑)** ✅ (第五批追加)
12. **系统配置表+首页快捷导航** ✅ (第六批追加)
13. **多数据库管理(界面维护连接)+实体级分库路由** ✅ (第六批追加，补完第 3 项的运行时遗留限制)
14. **新增数据库连接的报错自查性改善** ✅ (2026-08-24，用户实际使用时踩到证书/类型两个坑)
15. **修复业务表叫 `Sys_xxx` 时分库路由失效** ✅ (2026-08-24，用户在新库建 `Sys_Area` 生成页面后查询报错)
16. **修复表头筛选中文筛不出结果** ✅ (2026-08-24，字符串 in 拼字面量在 Latin1 排序规则库上丢中文)
17. **表格快捷复制(代码生成器列设置)** ✅ (2026-08-24，参考项目的 `initCopyBtn` 做成框架级列开关)

**当前状态：全部完成** 🎉

---

## 二、已完成功能

### 1. 表头筛选UI美化 ✅ (2026-07-30 完成，详见历史版本)

### 2. 多环境配置系统 ✅ (2026-07-30 完成)

- 3个环境配置文件（Development/Staging/Production），`run.bat dev/stg/prd` 快捷启动
- AppSetting.RunningEnvironment 环境标识，启动时输出环境横幅

### 3. CustomConsole + NLog日志系统 ✅ (2026-07-30 完成)

- `CustomConsole.WriteLine/WriteTable` 双重输出（控制台+文件），NLog 4类日志分目录落盘

### 4. 多数据库连接支持 ✅ (2026-07-31 完成)

**实现方式**: 参考项目是 EF 版（每库一个 DbContext 类，DBServer 字段存 DbContext 类名做 switch）。
本项目为 SqlSugar 版，映射为 **SqlSugarScope 多连接 ConfigId 模式**：DBServer 字段保存
appsettings 中 `Connections` 节点下的**连接名**（即 ConfigId），运行时按名称切换连接。

**后端相关文件**:
| 文件 | 改动 |
|---|---|
| `VOL.Core/Configuration/AppSetting.cs` | 新增 `Connections` 节点解析、`NamedConnection` 类、`GetConnection(name)`，支持DES解密 |
| `VOL.Core/DBManager/SqlSugarRegister.cs` | 新增 `GetAllConnectionConfigs()`（默认+全部命名连接），`UseSqlSugar()` 注册多连接 |
| `VOL.Core/DBManager/DbManger.cs` | 静态 SqlSugarClient 同样注册全部连接；新增 **`GetDbClient(dbServer)`**（核心方法：按名取连接，为空/未注册回退默认库，请求上下文不可用时用后台静态连接） |
| `VOL.Core/DBManager/DBServerProvider.cs` | 静态构造注册全部命名连接字符串 |
| `VOL.Core/DBManager/SqlSugarDbType.cs` | `GetType(name)` 支持传连接名（取其配置的DBType）或直接传类型名 |
| `VOL.Core/Infrastructure/DictionaryManager.cs` | 字典 DbSql 按 `DBServer` 切库执行 |
| `VOL.Sys/Services/System/Partial/Sys_DictionaryService.cs` | `GetVueDictionary/GetSearchDictionary/GetTableDictionary/GetPgSqlTableDictionary` 全部按字典的 DBServer 切库 |
| `VOL.Entity/DomainModels/System/Sys_Dictionary.cs` | DBServer 字段加 `[Editable(true)]`（否则表单不能保存该字段） |
| `VOL.Entity/DomainModels/Core/Sys_TableInfo.cs` | 新增 `DBServer` 字段 |
| `VOL.Builder/Services/Core/Partial/Sys_TableInfoService.cs` | `CreateEntityModel/SyncTable/InitTable` 按表的 DBServer 切库查表结构；`GetConnectionKey` 返回实际连接串（修复MySQL取库名）；生成实体时把 `DBServer` 写入 `[Entity(...)]` 特性 |

**前端相关文件**:
| 文件 | 改动 |
|---|---|
| `vol.web/src/views/sys/system/Sys_Dictionary/options.js` | 编辑表单加"所在数据库"下拉（绑定字典 `dbServer`），列表列取消隐藏 |
| `vol.web/src/views/builder/builderData.jsx` | 代码生成器主表单+新建弹窗加"数据库"下拉 |
| `vol.web/src/views/builder/coder.vue` | 加载字典 `dbServer` 填充下拉；新建表时传 `dbServer` 参数 |

**配置文件**: 4个 appsettings 均加了 `Connections` 节点及注释示例（默认为空=只有默认库）：
```json
"Connections": {
    "ReportDB": {
        "DBType": "MsSql",
        "DbConnectionString": "Data Source=REPORT_SERVER;Initial Catalog=Report;..."
    }
}
```

**数据库脚本**: `DB/sqlserver/升级脚本_20260730_多数据库支持.sql`（✅已在开发库执行）
- Sys_TableInfo/Sys_Dictionary 的 DBServer 列（幂等，本库已存在）
- 新增字典 `dbServer`（下拉数据源，含 Default 项；新增连接后在字典管理中加对应字典项即可）

**使用方法**:
1. appsettings 的 `Connections` 节点配置命名连接（如 ReportDB）
2. 字典管理 → 字典 `dbServer` 增加字典项（key=ReportDB）
3. 字典数据源sql指定"所在数据库"，或代码生成器建表时选择"数据库"
4. 不选/为空 = 默认库，未注册的名称自动回退默认库（兼容历史数据）

**E2E验证** ✅: 临时配置 TestDB 指向 master 库，字典 DbSql=`SELECT DB_NAME()` 返回 `master`，
证明真实切库执行成功（测试后已还原）。

### 5. 多角色支持 ✅ (2026-07-31 完成)

**实现方式**（与参考项目一致）: `Sys_UserRole` 中间表 + 权限并集合并。
**兼容设计**: 用户权限 = Sys_UserRole 中启用的角色 ∪ 主角色(Sys_User.Role_Id)。
主角色仍由用户编辑表单维护（不破坏现有单角色逻辑），额外角色通过"设置角色"弹窗管理。

**后端相关文件**:
| 文件 | 改动 |
|---|---|
| `VOL.Entity/DomainModels/System/Sys_UserRole.cs` | 新建中间表实体（Id/UserId/RoleId/Enable+审计字段，移除角色置Enable=0软删除） |
| `VOL.Entity/DomainModels/System/UserInfo.cs` | 新增 `int[] RoleIds`（保留 Role_Id 兼容） |
| `VOL.Core/UserManager/UserContext.cs` | `GetUserInfo` 加载全部角色；`RoleIds` 属性（旧缓存回退主角色）；`IsSuperAdmin` 任一角色为1即是；**`GetPermissions(int[])` 多角色权限并集合并**（按菜单去重、按钮权限取并集，每个角色仍走原单角色缓存）；`Permissions/GetPermissions(tableName)/GetPermissions(func)/ExistsPermissions` 全部走合并逻辑 |
| `VOL.Sys/Services/System/Partial/Sys_UserService.cs` | 登录返回 `roles` 数组；`GetUserRoles`（弹窗数据：当前角色+可分配角色，非超管只能分配下级角色）；`SaveUserRoles`（差量维护+越权校验+清用户缓存）；`GetPageData` 的 RoleName 显示全部角色（" / "分隔） |
| `VOL.Sys/IServices/System/Partial/ISys_UserService.cs` | 接口声明 |
| `VOL.WebApi/Controllers/Sys/Partial/Sys_UserController.cs` | `GET/POST /api/User/getUserRoles?userId=`、`POST /api/User/saveRole?userId=`（body为角色ID数组） |

**前端相关文件**:
| 文件 | 改动 |
|---|---|
| `vol.web/src/extension/sys/system/Sys_UserGridHeader.vue` | 新建"设置角色"弹窗（角色树多选，主角色锁定不可取消） |
| `vol.web/src/extension/sys/system/Sys_User.jsx` | 注册弹窗组件 + 工具栏"设置角色"按钮（选中一行后点击） |
| `vol.web/src/views/sys/system/Sys_User/options.js` | 列表显示"拥有角色"列 |

**数据库脚本**: `DB/sqlserver/升级脚本_20260731_多角色支持.sql`（✅已在开发库执行）
- 创建 Sys_UserRole 表（含索引），现有用户主角色已初始化进去

**接口验证** ✅:
- `getUserRoles?userId=1` → `{userRoleIds:[1], mainRoleId:1, roles:[角色树]}`
- `saveRole` → "角色设置成功"（差量无损）
- 用户列表 RoleName → "超级管理员"、"测试管理员"正常显示

**权限合并已覆盖的消费方**: 菜单加载(Sys_MenuService)、角色权限树(Sys_RoleService)、
接口权限过滤(ActionPermissionFilter) 均走 `UserContext.Permissions` → 自动生效。

**已知限制**（超出本次范围，如需可后续做）:
- 工作流按角色找审批人（WorkFlowManager 中 `Sys_User.Role_Id == roleId`）仍按主角色查找
- 用户列表的数据权限过滤（非超管只能看下级角色用户）仍按主角色过滤

### 6. SignalR功能 ✅ (2026-07-31 确认+补全)

**确认结果**: 前后端已基本完整——
- 后端: `HomePageMessageHub`（连接管理/定向发消息/强制下线）、Program.cs 已注册
- 前端: `@microsoft/signalr@8.0.7`、`MessageConfig.js` 自动连接+重连+通知、`/signalR` 演示页、`main.js signalR:true` 开关

**本次补全**:
| 文件 | 改动 |
|---|---|
| `VOL.WebApi/Program.cs` | MapHub 受 `Connection.UseSignalR` 配置开关控制（与参考项目一致）+ RequireCors；UseCors 调整到 UseRouting 之后（端点级CORS要求） |
| `VOL.WebApi/Controllers/Hubs/HomePageMessageHub.cs` | 新增静态 `SendMessageAsync(IHubContext, username, title, message)` 供业务代码服务端主动推送（username为空=推送全部在线用户，不入库） |

### 7. SignalR站内消息入库+已读未读 ✅ (2026-07-31 完成)

**实现方式**（与参考项目一致的两表设计）:
- `Sys_Message` 消息主表（一条消息一条记录）
- `Sys_MessageUser` 收件人表（**每个收件人一条已读状态记录**，IsRead/ReadDate）
- Hub 发送流程：admin 通过 `/signalR` 页面发送 → **先事务入库** → 再推送给在线连接；
  离线用户登录后在首页右上角铃铛看到未读消息

**后端相关文件**:
| 文件 | 说明 |
|---|---|
| `VOL.Entity/DomainModels/System/Sys_Message.cs`、`Sys_MessageUser.cs` | 新建实体 |
| `VOL.Sys/IRepositories(Repositories)/System/...MessageRepository...` | 新建仓储×4 |
| `VOL.Sys/IServices(Services)/System/(Partial/)...MessageService...` | 新建服务×8：`CreateMessageAsync`(事务写入两表)、`GetSentMessagesAsync`(发送记录含已读/未读人数)、`GetRecipientStatusAsync`(收件人阅读状态)、`GetMyMessagesAsync`、`GetMyUnreadCountAsync`、`MarkAsReadAsync`、`MarkAllAsReadAsync` |
| `VOL.WebApi/Controllers/Sys/(Partial/)Sys_MessageController.cs`、`Sys_MessageUserController.cs` | 接口：`GetSentMessages`、`GetRecipientStatus/{id}`、`GetMyMessages`、`GetMyUnreadCount`、`MarkAsRead/{id}`、`MarkAllAsRead` |
| `VOL.WebApi/Controllers/Sys/Partial/Sys_UserController.cs` | 新增 `GetMessageRecipients` 收件人下拉数据源 |
| `VOL.WebApi/Controllers/Hubs/HomePageMessageHub.cs` | `SendHomeMessage` 改为对象入参(支持多收件人)，发送前调用 `CreateMessageAsync` 入库 |

**前端相关文件**:
| 文件 | 说明 |
|---|---|
| `vol.web/src/views/index/MessageState.js` | 新建：消息共享状态(未读数/列表) |
| `vol.web/src/views/index/MessageConfig.js` | 重写：连接时加载未读数+消息列表；收到推送弹通知+桌面通知+从服务端刷新列表；重连后刷新未读数；保留强制下线逻辑 |
| `vol.web/src/views/index/Message.vue` | 重写：首页右上角铃铛红点=真实未读数；未读/已读两个页签；点开消息显示详情并自动标记已读；"全部已读"按钮 |
| `vol.web/src/views/signalR/Index.vue` | 重写：多收件人选择、发送记录（含已读/未读人数）、收件人阅读状态弹窗 |

**数据库脚本**: `DB/sqlserver/升级脚本_20260731_站内消息.sql`（✅已在开发库执行）

**E2E验证** ✅（全部通过）:
- Hub 发送 → 入库成功，离线收件人提示"用户登录后仍可查看"
- 收件人在线时实时收到推送（双连接实测）
- 未读数量 1→标记已读→0；全部已读 ✅
- 发送记录 readCount/unreadCount 统计正确；收件人阅读状态含阅读时间
- 开发库中保留了2条测试消息（admin666 的已读消息），可登录后在铃铛中查看效果

**注意**: 发送入口为 `/signalR` 页面（仅admin可发送，与参考项目一致）。

**UI优化(2026-07-31追加)**: 发送记录弹窗升级为 消息标题+内容预览、收件人数标签、阅读进度条(已读x/y)、
斑马纹表格、空状态提示；阅读状态弹窗升级为 消息内容卡片(含发送时间/收件人数/已读进度) + 收件人头像列表 + 已读/未读标签。

### 8. Sys_QuartzOptions 设置频率生成cron ✅ (2026-07-31 完成)

**实现方式**（照参考项目迁移）: 新建/编辑定时任务时，"Corn表达式"字段右侧
"**设置频率**"链接打开弹窗，选择 每小时/每天/每周/每月 + 时间点，自动生成
Quartz 6段cron表达式并回填；已有表达式可反解回显；保存时后端自动生成中文描述。

**相关文件**:
| 文件 | 说明 |
|---|---|
| `vol.web/src/extension/sys/quartz/CronBuilderDialog.vue` | 新建：频率选择弹窗（4种频率、生成/反解cron、实时显示表达式与中文说明） |
| `vol.web/src/extension/sys/quartz/Sys_QuartzOptions.jsx` | modelBody 挂载弹窗；CronExpression 的 extra 从外链改为"设置频率"；编辑时链接文字显示当前频率描述 |
| `vol.web/src/views/sys/quartz/Sys_QuartzOptions.vue` | 列表新增"执行频率"列(CronDescr) |
| `VOL.Entity/DomainModels/Quartz/Sys_QuartzOptions.cs` | 新增 CronDescr(频率中文描述)、CronStr(表达式副本) 字段 |
| `VOL.Sys/Services/Quartz/Partial/Sys_QuartzOptionsService.cs` | `GenerateCronDescr` 按cron生成中文描述，Add/Update 时自动写入 |

**数据库脚本**: `DB/sqlserver/升级脚本_20260731_定时任务频率描述.sql`（✅已在开发库执行）

**验证** ✅: API新建任务 cron `0 30 8 * * ?` → CronDescr 自动生成"每天08:30执行"（已删测试数据）。
cron 仍可手动输入，Quartz 调度器注册时会校验表达式合法性（框架原有逻辑）。

### 9. Sys_Application 多应用/子系统支持 ✅ (2026-08-03 完成)

**核心设计**（在参考项目基础上做了增强，2026-08-03 升级为 RootMenuIds 多根菜单绑定）:
- **角色属于应用**（`Sys_Role.AppID`，单值），用户通过**多角色**获得多个应用入口
- **应用绑定多个根菜单**（`Sys_Application.RootMenuIds`，逗号分隔）：应用的菜单范围 = **所有根菜单子树的并集**；
  加载菜单时所有根菜单被隐藏、其子菜单 parentId 置 0 提升为一级（"子系统一级菜单隐藏"）；
  **范围外的菜单即使角色误配了权限也不会混入**（子树圈定）；
  **公共子树可同时绑定到多个应用实现菜单共享**——同一菜单在不同应用中的按钮权限按"该应用下的角色"分别计算
  （如同一个"文件管理"在应用A可增删改、在应用B只读）。
  未绑定时回退按 AppName 同名一级菜单匹配（兼容参考项目的隐式约定）
- appId 通过 query 传递（`getTreeMenu?appId=`），当前选择存 localStorage（`current_app_id_{userId}` 按用户隔离）
- **定制首页**：`Sys_Application.DataPanel` 存组件名 → 首页动态加载 `src/views/home/{DataPanel}.vue`；
  Title 字段进入应用后设置浏览器标题
- **超级管理员**：不选应用=全量菜单（默认）；也可从 guide/"切换应用"进入任意子系统，
  按该应用子树视角查看（拥有子树内全部菜单与按钮权限）；guide 页给超管专属"完整菜单"卡片随时切回全量视图；
  权限分配页不受影响（仍是完整树）
- **注意**: 接口层权限校验(ActionPermissionFilter)仍是用户所有角色的权限并集（跨应用取最大），按应用区分的是菜单/按钮显示

**后端相关文件**:
| 文件 | 说明 |
|---|---|
| `VOL.Entity/DomainModels/System/Sys_Application.cs` | 新建实体(AppID/AppCode/AppName/Title/Icon/Theme/PrimaryColor/DataPanel/SortOrder/Enabled) |
| `VOL.Entity/DomainModels/System/Sys_Role.cs` | 新增 `AppID` 字段(所属应用，0=未指定) |
| `VOL.Sys/.../Sys_ApplicationRepository、Service、Controller` | 标准四件套 + `GET api/Sys_Application/GetEnabledApps`(按当前用户角色过滤) |
| `VOL.Sys/Services/System/Partial/Sys_RoleService.cs` | `GetAppIdsByRoleIds`/`GetRoleIdsByAppId`；角色 Update 时 **AppID 级联到所有子角色** |
| `VOL.Sys/Services/System/Partial/Sys_MenuService.cs` | `GetCurrentMenuActionListByAppId(appId)`：按应用缩小角色→查权限→隐藏应用同名一级菜单；`GetMenuActionList(int[],appName)` 多角色并集；菜单查询整体改为多角色合并 |
| `VOL.WebApi/.../Sys_MenuController.cs` | `getTreeMenu?appId=`；**非超管不带 appId 返回空菜单**(防泄露全量菜单) |
| `VOL.Sys/Services/System/Partial/Sys_UserService.cs` | 登录返回 `isSuperAdmin/appIds/userId`(角色应用 ∩ 启用应用) |

**前端相关文件**:
| 文件 | 说明 |
|---|---|
| `src/config/appConfig.js` | 新建：应用列表API、appId 按用户持久化(localStorage) |
| `src/store/index.js` | 新增 appList/currentAppId/userAppIds 状态与 initAppList action |
| `src/views/Guide.vue` | 新建：子系统选择页(渐变星空背景+应用卡片流光动效+无权限提示) |
| `src/router/index.js` | `/guide` 路由；守卫：非超管已登录未选应用强制进 guide |
| `src/views/Login.vue` | 登录跳转分支：超管→首页；单应用→自动选中进首页；多应用→guide；无应用→提示 |
| `src/views/index/IndexMethods.js` | 菜单请求带 `?appId=`；进入应用后按 Title 设置浏览器标题 |
| `src/views/Index.vue` | 头部用户菜单新增"切换应用"入口(回 guide 重选) |
| `src/views/Home.vue` + `src/views/home/DataPanelDefault.vue` | 首页支持按应用动态加载定制面板(DataPanel 字段)，附示例面板组件 |
| `src/views/sys/system/Sys_Application.vue` + extension + viewGird 路由 | 应用管理页面(增删改查) |
| `src/views/sys/system/Sys_Role/options.js` | 角色表单/列表增加"所属应用"下拉(字典 `application`，数据取自 Sys_Application) |

**数据库脚本**: `DB/sqlserver/升级脚本_20260731_多应用支持.sql`（✅已在开发库执行）
- Sys_Application 表 + 默认应用种子；Sys_Role.AppID 列(默认0)；字典 `application`(DbSql取自应用表)；"应用管理"菜单(挂在角色管理同级)

**E2E验证** ✅（自签JWT实测，演示应用绑定两棵子树 RootMenuIds='61,32' 即"系统设置"+"基础组件"）:
- GetEnabledApps: admin→全部应用；admin666(角色2→应用1)→应用1
- 非超管不带 appId → menu=[] ✓
- **超管不带 appId → 84个全量菜单**；**超管带 appId=1 → 38个(两棵子树并集)，两个根都隐藏、两棵树的子菜单全部提升为一级、拥有全部按钮权限** ✓
- **普通用户带 appId=1 → 26个：子树并集∩角色权限**；单根时被圈出去的"基础组件"子树权限菜单，
  把32加入应用根菜单后重新出现（多应用共享公共子树的机制验证）✓

**当前开发库演示配置**: 应用1 绑定根菜单"61,32"(系统设置+基础组件)、角色2(测试管理员)归属应用1。

**使用步骤**:
1. 应用管理中新建应用，**"根菜单"多选下拉选择该子系统的一级菜单（可多选）**——应用名随意取；
   公共模块整理成独立一级菜单（如"公共功能"），哪个应用需要就选给哪个应用
2. 角色管理中给角色选择"所属应用"（子角色自动跟随父角色的应用）
3. 用户通过"设置角色"(多角色)获得多个子系统入口，登录后多应用会进入 guide 选择页
4. 需要定制首页时：把前端组件放到 `src/views/home/`，DataPanel 字段填组件名（示例：DataPanelDefault）

**注意**: 老角色 AppID=0 不贡献任何应用——非超管用户如果所有角色都未指定应用，登录会提示"没有任何应用权限"；
升级后请给现有角色指定所属应用。

### 10. 多语言(国际化)支持 ✅ (2026-08-04 完成，参考 C:\vol.pro.ts_vite-master)

**实现方式**（与 pro 版一致的数据库驱动方案，而非旧版"改 en.js 文件"方案）:
- **前端基础设施 2026.05 升级包已自带**（本次仅补缺）: `uitils/translator`($ts/$tst，启动时按当前语言 GET `lang/{lang}.js` 加载进 store)、
  `components/lang`(切换下拉，localStorage `vol_lang`)、App.vue el-config-provider 已接 element-plus locale、
  框架组件 43 个文件已调用 $ts、Index/Login 已挂 `<lang>` 组件、index.html 已有 `lang_storage_key`
- **本次新增·前端**: `main.js` 开启 `$global.lang: true`（不开则切换入口隐藏）、
  `views/sys/lang/Sys_Language.vue` 语言设置页 + `extension/sys/lang/Sys_Language.js`（含[生成语言包]按钮）、路由 `/Sys_Language`
- **本次新增·后端**（vol.api.sqlsugar，pro 项目仅有前端，后端按本框架模式全新实现）:
  - `Sys_Language` 实体(ZHCN 为翻译 key + ZHTW/English/French/Spanish/Russian/Arabic 翻译列)
  - Repository/IService/Service/Controller 全链路 + `GET api/Sys_Language/createLanguagePack`
  - 生成语言包：查全表 → 每种语言写 `wwwroot/lang/{zh-tw,en,fr,es,ru,ar}.js`（纯 JSON、UTF-8 无 BOM）；
    wwwroot 根静态文件服务已有(`ServeUnknownFileTypes=true`)，前端直接 GET `lang/en.js`
- **数据**: `升级脚本_20260804_多语言支持.sql`（表+菜单"语言设置"挂系统管理下+种子 290 条中英翻译，种子来自前端 uitils/translator/en.js）
- **使用**: 语言设置页面维护翻译 → 点[生成语言包] → 登录页/顶部导航切换语言(页面 reload 生效)；
  未维护翻译的 key 显示中文原文；简体中文模式不加载语言包（$ts 直接返回 key）

**代码生成器界面对比结论**（用户同题询问）: pro 版 coder.vue(655行) 的功能当前项目 coderV2 系列(2900+行)全部覆盖且更多
（明细表选择弹窗、排序设置弹窗、快捷查询字段、多数据库下拉、行拖拽排序、查询/编辑表单可视化切换视图）；
pro 版独有的 apiInPut/apiOutPut/columnformat/script/import 列在其源码中全部为**注释掉的"待实现"代码**，无实际功能，**无可移植项**。
表单拖拽设计器(VolFormDraggable)两边均有。

### 11. 登录页重设计+去验证码+多语言问题修复 ✅ (2026-08-05 完成)

- **去掉登录验证码**：`Sys_UserService.Login` 的 verificationCode 参数原本从未生效(无条件校验)，现已实现开关逻辑；
  `Sys_UserController.Login` 传 `verificationCode: false` 关闭校验(要恢复改回 true 并还原前端验证码输入框)；
  `ValidationContainer.cs` Login 校验器不再要求 VerificationCode/UUID；getVierificationCode 接口保留未删
- **登录页重设计**：左侧品牌区(渐变+特性点)+右侧表单卡片，密码可见切换，移除演示账号/QQ群/备案号等演示链接，
  文案全部走 $ts(新增词条：企业级快速开发平台/登录中/正在登录 等，DB 294 条)
- **修复切换语言不生效(根因 CORS)**：语言包 `lang/{lang}.js` 是跨域 XHR(带 Authorization 头触发 OPTIONS 预检)，
  而 UseCors 原在 UseStaticFiles 之后 → 静态文件无 CORS 头被浏览器拦截。已在 Program.cs 将 `app.UseCors("cors")`
  提前到 UseStaticFiles 之前(实测预检 204+Allow-Origin 正常)
- **首页顶部语言切换入口**：Index.vue 中被注释的 `<lang :color="color">` 已恢复(color 随主题自适应)并补 import
- **超管菜单"扁平化"问题**：非 bug——多应用功能的"应用子树视角"(根菜单隐藏子菜单提升)，因 localStorage 存了
  之前在 guide 页选过的 appId。修复：超管登录成功时 `removeSavedAppId()` 清除应用视角，默认加载全量菜单；
  中途仍可从头像下拉"切换应用"进入子系统视角(本次会话内保持)

### 12. 删除 EF 版后端 + 代码生成器编辑模式 ✅ (2026-08-10 完成)

- **`vol.api/`(EF 版)整体删除**：实际使用的后端只有 `vol.api.sqlsugar`，双份后端维护成本高且容易改错目录。
  **套官方升级包时必须跳过 `vol.api/`**。
- **代码生成器编辑模式** `Sys_TableInfo.EditType`：0/null=弹出框、1=新页面(`newTabEdit:true`)、2=表格行内编辑(`editTable:true`)。
  生成逻辑 `Sys_TableInfoService.CreateVuePage`(`editLine` 变量 + `{$false}`/`{#editTable}` 替换)、
  模板 `VOL.WebApi/Template/Page/VueOptions.html`；运行时 `components/basic/ViewGrid/ViewGridEditTable.js`
  (loadTableAfter 快照 → 保存时比对脏行逐行调 add/update，按 Add/Update 权限门控)，
  接线在 `ViewGridEvent.js`/`ViewGrid.vue`(`initEditTable`)/`ViewGridFilter.js`(新钩子 tableEditSaveBefore/After)。
  演示：开发库 `MES_Customer`(客户管理) EditType=2。
  **坑**：`EditType` 只在**生成页面时**读取，改完必须重新生成页面；页面 options.js 缺 `editTable:true`
  则 `initEditTable` 直接 return(能进编辑态但没有保存按钮)。

### 13. 系统配置表 Sys_ConfigSetting ✅ (2026-08-20 完成)

`ConfigKey` 唯一的键值对配置表。首个用途：代码生成器的生成路径 `builder.vuePath`/`builder.appPath`
从前端 localStorage 改为存库——**原因**：复制框架做新项目时同一个 localhost 域名下会读到旧项目的路径，
代码会生成到错误的目录。接口 `BuilderController.GetBuilderPaths`(登录可读)/`SaveBuilderPaths`(限超管，
路径决定文件写到哪)。脚本 `升级脚本_20260820_系统配置表.sql`。

### 14. 首页快捷导航 ✅ (2026-08-20 完成)

`/home` 页的用户自定义快捷菜单，支持拖拽排序。
- 表 `Sys_UserShortcut`(UserId+AppId+MenuId 唯一索引)，**只存 MenuId 不存 name/url/icon**：
  显示信息由前端从 `store.state.permission` join 出来 → 菜单改名自动跟随、权限被收回时快捷项自动消失、零额外请求。
  `MenuName` 是纯排障用的冗余列，`AppId=0` 表示不区分应用。
- 后端 `Sys_UserShortcutService.cs`：GetMyShortcuts / AddShortcut(一次多个、上限 30、校验菜单在权限范围内——
  复用 `Sys_MenuService.GetCurrentMenuActionListByAppId`，天然带多应用+多角色逻辑) / RemoveShortcut /
  SaveSort。所有接口只操作 `UserContext.Current.UserId` 自己的数据。
- 前端 `views/home/HomeShortcut.vue` + `views/home/DataPanelDefault.vue`，在 `Home.vue` 中引入。
- **权限约定**：`ApiBaseController` 类级已有 `[JWTAuthorize]`，自定义 action 不加 `[ApiActionPermission]`
  就只校验 JWT、不校验菜单权限 → 这张表不用在菜单里建权限记录，所有登录用户可用(同类先例 `GetEnabledApps`)。
- **坑**：`vue-draggable-next` 只在挂载时读一次 Sortable 配置，`:disabled` 动态改不生效 → 用 `updateOptions`；
  `getSavedAppId()` 未选应用时返回 null(与默认为 1 的 `store.state.currentAppId` 不同)，所以用 `getSavedAppId() || 0`。

### 15. 多数据库管理(界面维护连接) ✅ (2026-08-21 完成)

界面上新增数据库连接，**不用改 appsettings.json、不用重启**，与功能 4 的 `Connections` 节点合并生效。
- 表 `Sys_DbConnection`(ConnName 唯一)，连接串按 `Secret.DB` 做 DES 加密落库，列表/编辑时密码显示为 `******`
  (未改动则保持原值)。保存前强制 `TestConnection` 通过才允许入库。
- 注册中枢 `VOL.Core/DBManager/DbConnectionManager.cs`：`Initialize()`(Program.cs 中在 `AppSetting.Init` 之后、
  `UseSqlSugar` 之前，把库里启用的连接合并进 `AppSetting.Connections`，appsettings.json 同名连接优先；
  **表不存在时静默跳过**，不能因此起不来)、`RegisterRuntime()`(界面新增/改连接串后立即注册，当次请求即可用)、
  `TestConnection()`(带 20 秒墙上时钟上限——连接串里 `Connect Timeout=500` 会把请求挂死)。
- **只增不删是硬性要求**(删掉会导致项目崩、代码生成失败)。三层兜底：菜单 Auth 没有 Delete、
  Service 覆写 `Del`/`DelAsync` 直接返回错误、`DbConnectionManager` 不提供任何移除 API。
  连接名 = ConfigId，被 `[Entity(DBServer)]`/字典 DBServer/`Sys_TableInfo.DBServer` 引用，
  **保存后也不允许改名**(前端 `readonlyUpdate` + 后端拒绝)。停用走 `Enabled=0`。
- 前端 `views/sys/system/Sys_DbConnection.vue` + `extension/sys/system/Sys_DbConnection.jsx`
  (view 按钮"已注册连接"、box 按钮"测试连接")。脚本 `升级脚本_20260821_多数据库管理.sql`。
- **SqlSugar 坑**：`SqlSugarScope` 是"一个异步上下文一份连接"，`AddConnection` 只对当次请求生效，
  下个请求会按私有字段 `_configs` 重建 → 运行时注册**必须反射往 `_configs` 里也写一份**；
  `RemoveConnection` 要求当前上下文已实例化过该连接，否则内部空引用 → 先 `GetConnection(id)` 再 Remove。

### 16. 实体级分库路由(补完功能 4 的遗留限制) ✅ (2026-08-21 完成)

功能 4 只做到"字典 + 代码生成器查表结构"切库，**生成出来的业务模块运行时仍走默认库**
(代码生成器早就把表的 DBServer 写进了 `[Entity(DBServer=...)]` 特性，但运行时没有人读它——
从 B 库生成的模块跑起来查的是默认库：默认库没这张表就报错，有同名表则静默写错库)。本次把这条链路补上。

**唯一路由收口点** `VOL.Core/DBManager/EntityDbRouter.cs`：`Route(entityType, client)` 只在传入
`SqlSugarScope`(注册了全部连接的总入口)时切换，调用方已明确指定具体连接(`SqlSugarProvider`)时保持原样不抢方向盘。

| 文件 | 改动 |
|---|---|
| `VOL.Core/DBManager/EntityDbRouter.cs` | 新建：`GetDbServer/GetClient/Route/IsRouted`，框架表白名单 + 主子表同库校验 |
| `VOL.Core/BaseProvider/RepositoryBase.cs` | `DbContext`/`SqlSugarClient`/`GetClient<T>()` 按实体路由；`GetTransactionClients()` 业务库优先；`UpdateRange`/`UpdateDetail` 按被更新实体分别路由；`SaveChanges` 双提交 |
| `VOL.Core/DbContext/BaseDbContext.cs` | `GetClient<TEntity>()`/`Set<TEntity>()`/`SaveChanges<TEntity>()` |
| `VOL.Core/DBManager/SqlSugarExtension.cs` | 所有 Add/Update/Set 路径接入 `EntityDbRouter.Route` |
| `VOL.Core/Extensions/IdentityCode.cs` | 流水号按实体所在库取最大值 |
| `VOL.Core/WorkFlow/WorkFlowManager.cs` | 审批写回状态时给业务库队列补一次 `SaveChanges<T>()` |
| `VOL.Core/BaseProvider/ApplicationServiceBase*Extensions.cs` | 增删改查全部走 `dbContext.GetClient<TEntity>()` |
| `VOL.Core/DBManager/SqlSugarDbType.cs` | 未注册的连接名回退**默认库类型**(而不是硬编码 SqlServer) |
| `VOL.Builder/.../Sys_TableInfoService.cs` | 生成代码时校验主子表同库，`NormalizeDbServer` 与运行时同一套归一化规则 |
| `VOL.Sys/.../Sys_DictionaryService.cs` | `GetTableDictionary` 的 in 条件改为一值一占位符 + 按字典自己库的类型转义 `key` |

**两条硬性规则**：
1. `VOL.Entity` 程序集里的 `Sys_*` 框架表强制留默认库(权限/菜单/字典/工作流都依赖它们，被误路由等于系统瘫痪；
   官方 `Sys_User` 自带 `DBServer="SysDbContext"` 历史值，一旦有人恰好建了同名连接，用户表就被指到别的库)
2. **主表与所有层级明细表必须同库**：跨库没有事务，主表提交、子表失败就是脏数据 → 直接抛异常而不是回退默认库
   (回退等于把子表写进另一个库，比报错更难查)。校验递归到二、三级明细——删主表是"先删三级、再删二级"，
   只校验直接子表的话三级配错库会在校验之前就把 DELETE 发到别的库去。生成代码时也拦一次，给可操作的提示。

**归一化规则各层必须一致**：空白 / `default` / **未注册的连接名**都视为默认库
(`EntityDbRouter.Normalize`、`Sys_TableInfoService.NormalizeDbServer`、`DbManger.GetDbClient`)。
这是为了兼容 `ServiceDbContext`/`SysDbContext` 历史值；按字面比较会把"主表历史值 + 明细空值"误判成跨库而生成不了代码。
`SqlSugarDbType.GetType` 同步回退：实体退回默认库时**方言必须一起退回**，否则默认库是 mysql/pgsql 却按 sqlserver 拼 sql。

**事务与提交**：`AddQueue`/`SaveQueues` 按连接分开 → `SaveChanges` 双提交(先业务库、`Queues.Count>0` 时再默认库)；
跨库是**链式本地事务**(无分布式事务)，业务库先提交，回滚用 `RollbackQuietly` 不吞原异常。
典型表现：业务数据落 ShardDb、`Sys_Log` 仍落默认库。

**缓存策略**：`EntityDbRouter` 只缓存"反射特性 + 同库校验"的结果，**不缓存"连接是否已注册"**——
数据库管理页可运行时新增连接(功能 15)，缓存了会让新连接一直不生效。

**E2E 验证** ✅(临时连接 `ShardDb` 指向 tempdb，实体 `MES_Bom_Main`/`MES_Bom_Detail`/`TestService` 配 `DBServer="ShardDb"`)：
- getPageData / getPageDataAsync / getDetailPage / add / addAsync(主+明细) / update(同一次请求里改+增+删明细) /
  del / delAsync(级联，0 孤儿) / export(200，3029字节) / getColumnDistinctValues(表头筛选) 全部通过，数据全部落 tempdb
- **无泄漏**：`vol_v3` 中同名的三张表未被触碰(MAX(CreateDate) 仍是 2025-03-19 / 2023-05-19)
- **双提交**：同一批请求的 `Sys_Log` 写进了 vol_v3，业务数据在 tempdb
- **回滚**：故意让明细插入数值溢出，主表停在 `Edition=V2`(不是尝试写入的 V9-ROLLBACK)，原明细完好
- **流水号**：`IdentityCode.Create` 生成 `TS202608210001/0002/0003`，按 ShardDb 自己的行自增(默认库 TestService 未动)
- **同库规则双向验证**：去掉 `MES_Bom_Detail` 的 DBServer → getPageData 报预期异常
  `主表[MES_Bom_Main]所在数据库为[ShardDb],子表[MES_Bom_Detail]为[默认库]...`；代码生成器侧返回可操作提示
- **历史值兼容**：`MES_Customer`(`ServiceDbContext`，未注册)照旧从默认库返回 5 行；`GetRegistered` 列出 default + ShardDb
- **字典多库**：GetVueDictionary / GetTableDictionary / GetSearchDictionary 均返回正确中文数据
  (修复前 `GetTableDictionary` 因 Dapper 式 `in @data` 报 `Incorrect syntax near '1c19c214-...'`)

**未覆盖**：审批流写回状态到分库实体未实测——现有唯一配了流程的表 `MES_ProductionReporting` 在默认库，
分库的三张表没有 `AuditStatus` 列，要测得改库结构/流程数据(需先经用户同意)。代码路径按同一套 `SaveChanges<T>()` 收口，仅做了阅读复核。

**本次为纯代码改动，无数据库变更**(实体特性上的 `DBServer` 由代码生成器写入)。
验证完成后测试配置已全部还原：三个实体的 `DBServer="ShardDb"` 撤回，`appsettings.json` 的
`Connections.ShardDb` 注释掉（保留为可用示例，需要回归验证时取消注释即可）。
还原后已复测：只有默认库注册时，`MES_Bom_Main`/`MES_Customer` 等带历史 DBServer 值的表照旧从默认库返回数据。

### 17. 新增数据库连接的报错自查性改善 ✅ (2026-08-24 完成)

用户在"数据库管理"页新增连接连续踩了两个坑，报错信息都指不到病根，只能靠人来猜：

| 用户看到的报错 | 真实原因 |
|---|---|
| `SSL出错…证书链是由不受信任的颁发机构颁发的` | `Microsoft.Data.SqlClient` 4.0+ 默认 `Encrypt=True` 且校验服务器证书，公司这台 SqlServer 用的是自签证书 → 连接串必须加 `TrustServerCertificate=True;` |
| `不支持的关键字:"user id"` | 连接串本身没错(实测同一份串 `MsSql` 一测就通)。这句是**驱动层的"关键字不认识"**：要么 [数据库类型] 选的不是 SqlServer，别的驱动去解析了 SqlServer 语法的串；要么键名里混进了**肉眼看不见的字符** |

第二个错的定位过程(逐 dbType 对同一份连接串打点)：`MsSql`/`SqlServer`/空值 → 连接成功；
`Kdbndp` → `Keyword not supported: data source`(与用户那句是同一族，只是中文本地化了)、
`PgSql`/`GaussDB` → `Couldn't set data source`、`MySql`/`OceanBase` → `Option 'trustservercertificate' not supported`、
`Oracle` → `ORA-50008`、`Dm` → `initial catalog does not exist`。
另一路复现：把 `User ID` 里的空格换成不换行空格(U+00A0)、全角空格、零宽空格、换行，
`Microsoft.Data.SqlClient` 一律报 `不支持的关键字:"user id"`——**打印出来和正常的完全一样**，
这是从文档/聊天工具复制连接串时最容易中的一枪。

| 文件 | 改动 |
|---|---|
| `VOL.Core/DBManager/DbConnectionManager.cs` | 新增 `NormalizeConnectionString`：只规范化等号左边的键名(零宽字符删掉、各类空白统一成单个普通空格)，**值一律不动**(密码里可能就有空格)；`TestConnection`/`RegisterRuntime`/`LoadFromDb` 三处入口全部先过一遍；`TestConnection` 失败时追加 `Suggest()` 生成的可执行建议 |
| `VOL.Core/DBManager/SqlSugarDbType.cs` | 新增 `IsSupportedName()`——`GetType` 对不认识的类型名是**静默回退默认库类型**的(为兼容历史 `DBServer` 值)，保存连接时不能跟着静默 |
| `VOL.Sys/.../Sys_DbConnectionService.cs` | 保存与测试两条路径都校验 `DBType` 必须是已支持的类型名；连接串落库前先 `NormalizeConnectionString` |
| `vol.web/.../Sys_DbConnection.vue` | 连接串占位符改成可直接照抄的 SqlServer 示例，并写明自签证书要加 `TrustServerCertificate=True` |
| `vol.web/.../Sys_DbConnection.jsx` | 测试连接前先要求选类型(空类型后端会回退默认类型，测出来的"成功"未必是你要连的库)；失败改用 `$alert` 弹框(证书类报错很长，`$message` 会截断且不能复制)；`modelOpenAfter` 的 `DBType` 兜底从"仅新增"扩到编辑(历史数据类型为空时会被新加的校验拦下) |

**为什么建议写在后端而不是前端**：保存路径(`CheckAndNormalize`)和测试按钮是两个入口，
错误文案只在按钮里拼的话，直接点保存的人看不到。

**E2E 验证** ✅(自签 JWT 调 `POST /api/Sys_DbConnection/TestConnection`)：
- 不可见字符：`User ID` 中的 U+00A0 / 全角空格 / 零宽空格 / 换行四种变体，改前全部 `不支持的关键字:"user id"`，改后**全部连接成功**
- 证书建议：不带 `TrustServerCertificate` 的原串 → 报错尾部追加 `【建议】服务器用的是自签证书，在连接字符串末尾加上 TrustServerCertificate=True;`
- 类型选错建议：`Kdbndp`/`PgSql`/`Oracle`/`MySql` 四种 → 均追加 `【建议】连接字符串是SqlServer的写法(Initial Catalog/TrustServerCertificate等)，但[数据库类型]选的是xxx，请改选SqlServer`
- 非法类型名 `Foo` → `不支持的数据库类型Foo,请从下拉中选择`(改前会被静默当成默认库类型)
- 正常串 + `MsSql` → 连接成功(未回归)

**未做**：没有在后端"自动纠正"数据库类型(比如发现是 SqlServer 语法就强行按 SqlServer 连)——
类型决定的是整个方言，猜错比报错危害大得多，只给建议。

**本次为纯代码改动，无数据库变更。**

### 18. 修复业务表名叫 `Sys_xxx` 时分库路由失效 ✅ (2026-08-24 完成)

**现象**（用户实际使用）：界面上新增数据库连接 `shelflife` 成功，在该库里建表 `Sys_Area`，
代码生成器能正常读出字段并生成页面，但打开页面查询 `api/Sys_Area/getPageData` 返回
`{"message":"服务器处理异常","status":false}`，重启后端也一样。

**真实报错**（自签 JWT 复现 + `ExceptionHandlerMiddleWare` 在非 Development 下会把异常统一换成
"服务器处理异常"，原文只在 Development 的响应里和 `Logs/Error` 里）：

```
Microsoft.Data.SqlClient.SqlException: Invalid object name 'Sys_Area'.
```

即：查询发到了**默认库 vol_v3**，而这张表只在 `shelflife` 库里。生成的实体本身是对的
（`[Entity(TableCnName="Sys_Area",TableName="Sys_Area",DBServer="shelflife")]`，DBServer 由代码生成器自动写入）。

**根因**：功能 16 的 `EntityDbRouter.IsFrameworkEntity` 判"是不是框架表"用的是
`名字以 Sys_ 开头 && 定义在 VOL.Entity 程序集里`。但**代码生成器把业务实体也生成到
`VOL.Entity/DomainModels` 下**（同一个程序集），所以用户这张业务表两个条件全中，
被当成框架表强行钉在默认库上——而框架表留默认库是硬规则、不看 DBServer，于是 DBServer 被无声丢弃。

| 文件 | 改动 |
|---|---|
| `VOL.Core/DBManager/EntityDbRouter.cs` | `Sys_` 前缀判断改为**写死的框架表名单** `_frameworkTables`(27 张，权限/菜单/字典/日志/消息/多语言/应用/配置/连接/代码生成器/定时任务/工作流)；新增 public `IsFrameworkTableName(tableName)` 供别处复用；`IsFrameworkEntity` = 名字在名单里 **且** 在 `VOL.Entity` 程序集里 |
| `VOL.Core/DBManager/SqlSugarRegister.cs` | Oracle 自增序列分支同样是 `Sys_` 前缀判断("框架表按 `T_{表名}_SEQ` 取序列")，业务表叫 `Sys_xxx` 时会被指定一个不存在的序列导致插入直接失败 → 改用 `EntityDbRouter.IsFrameworkTableName` |
| `VOL.Builder/.../Sys_TableInfoService.cs` | `ValidColumnString` 开头新增重名拦截：表名真的和框架表撞名时(如 `Sys_Log`)，生成的实体会与框架实体同名同命名空间导致编译不过，且强制默认库，直接给出"请把业务表改个名字再生成"而不是让人事后排查 |

**为什么用名单而不是继续用前缀**：前缀判断有两个方向都会错——业务表叫 `Sys_xxx` 被误判为框架表（本次的坑），
而官方升级包新增的 `Sys_*` 表不在名单里也不会出问题（它们本来就没配 `DBServer`，`Normalize` 后就是默认库）。
名单是"只在真正重名时才生效"的最小拦截面。

**E2E 验证** ✅（`shelflife` 连接 + 用户自己建的 `Sys_Area` 表，自签 JWT）：
- `Sys_Area/getPageData` → total=11，中文字段完好（改前 `Invalid object name 'Sys_Area'`）
- 增删改一轮：add(ID 12，CreateID/Creator 自动填充) → update(`路由验证区域-改`，保存成功) → del(删除成功) → total 回到 11，
  **未改动用户原有数据**
- 表头筛选 `getColumnDistinctValues` → 5 个去重值 / total 11；导出 `export` → 200，2983 字节
- `GetRegistered` → `default` + `shelflife` 两个连接均已注册
- **框架表未受影响**（仍在默认库）：`Sys_User` 2、`Sys_Dictionary` 35、`Sys_Role` 3、`Sys_DbConnection` 1、
  `Sys_Log` 1032→1070（说明分库表的操作日志照旧落默认库）、`Menu/getPageData` 95、`Menu/GetTreeMenu` 正常
- 重名拦截：代码生成器对 `Sys_Log` 返回
  `表名[Sys_Log]与框架自带的表重名,生成的实体会与框架实体冲突(且框架表强制使用默认库),请把业务表改个名字再生成`
- `dotnet build` 0 错误；`npm run build` ✓ 26.40s

**本次为纯代码改动，无数据库变更**（`Sys_Area` 表和 `shelflife` 连接都是用户自己在界面上建的，未由本次改动创建或修改）。

### 19. 修复表头筛选中文筛不出结果 ✅ (2026-08-24 完成)

**现象**（用户实际使用）：表头筛选勾选英文/数字的值能正常筛出数据，勾选**中文**值时结果为空。

**定位**：开 `ShowSqlLog=1` 看实际发出的 sql，`in` 查询生成的是把值直接拼进语句的字面量：

```sql
-- 中文(筛不出)                            -- 数字(能筛出)
WHERE ([DicName] IN ('性别'))              WHERE ([Code] IN ('8'))
```

在数据库上直接对比，病根一目了然：

```sql
SELECT CAST('性别' AS VARBINARY(20));   -- 0x3F3F        ← 两个问号
SELECT CAST(N'性别' AS VARBINARY(20));  -- 0x27602B52    ← 真正的中文
SELECT COUNT(*) FROM Sys_Dictionary WHERE DicName IN ('性别');   -- 0
SELECT COUNT(*) FROM Sys_Dictionary WHERE DicName IN (N'性别');  -- 2
```

**根因**：SqlServer 对**不带 `N` 前缀**的字符串字面量按数据库排序规则的代码页转换，本项目的库是
`SQL_Latin1_General_CP1_CI_AS`（非 Unicode），中文字面量在进入比较之前就已经变成 `??`，
所以条件永远不成立。列本身是 `nvarchar`、数据也完好，**纯粹是"值怎么送进数据库"的问题**。
`like` 一直正常，是因为 `Contains` 生成的是 `like '%'+@参数+'%'`——走参数就没有代码页转换。

用 SqlSugar 5.1.4.214 逐种写法打点确认（`ToSql()` 看语句与参数）：

| 写法 | 生成的 sql | 中文 |
|---|---|---|
| `list.Contains(x.Col)`（框架原来的 in） | `IN ('性别','状态')` 字面量，0 个参数 | ✗ 丢失 |
| `.In(it=>it.Col, values)`（SqlSugar 原生） | `IN ('性别','状态')` 字面量 | ✗ 丢失 |
| `x.Col == value` | `= @Col0`，参数 DbType=String | ✓ |
| 等值 or 链 | `(=@Col0 OR =@Col1)`，n 个参数 | ✓ |

`ConnMoreSettings.DisableNvarchar` 只影响**参数**的 DbType(String→AnsiString)，对字面量 `IN` 没有任何作用，
所以不是配置能绕过去的。

| 文件 | 改动 |
|---|---|
| `VOL.Core/Extensions/LambdaExtensions.cs` | `GetContainsExpression` 里字符串字段改走新的 `GetStringInExpression`：把 in 拆成参数化的等值 or 链 `p=>p.Col==v1\|\|p.Col==v2`(notIn 外面套 `Not`)；值全被过滤掉时返回 `False<T>()`/`True<T>()` 而不是无条件全表。非字符串列(int/日期/Guid)保持原来的 `IN` 不变 |
| `VOL.Entity/.../LoadSingleDataModel.cs` | `SearchParameters` 新增 `List<string> Values`：`Value` 是逗号拼接的，值本身含逗号(如地区名 `北京市,新疆`)会被拆成两个值而匹配不到 |
| `VOL.Core/.../ApplicationServiceBaseSearchExtensions.cs` | `in`/`notIn` 优先取 `Values`，没传时仍按 `Value` 拆逗号(兼容所有老页面、`wheres` 字符串写法和工作流筛选)；`Values` 有值时不再要求 `Value` 非空 |
| `vol.web/.../VolTable.vue` | `applyColumnFilter` 的 in 条件同时提交 `Value`(兼容) 和 `Values` 数组 |

**为什么不改数据库排序规则**：改排序规则影响全库所有字符串比较与索引，代价和风险都远大于问题本身；
而且框架要能跑在别人的库上，不能要求用户的库必须是 Unicode 排序规则。参数化是唯一正确的修法。

**为什么不是"把字面量加上 N 前缀"**：那要改 SqlSugar 生成 sql 的行为(方言层)，且只能解决 SqlServer；
参数化对所有数据库都对。

**E2E 验证** ✅（自签 JWT，分库表 `Sys_Area`@shelflife + 默认库框架表）：

| 场景 | 改前 | 改后 |
|---|---|---|
| `in` 单个中文 `福建省` | total=0 | total=1 ✅ |
| `in` 两个中文 `福建省,广东省` | total=0 | total=2 ✅ |
| 默认库 `Sys_Dictionary` in `性别` | total=0 | total=2 ✅ |
| `Sys_Menu` in `数据字典` / `Sys_User` in `超级管理员` | — | total=2 / total=1 ✅ |
| `in` 数字 `8` / 英文 `admin` | 正常 | 仍正常 ✅ |
| `like` 中文 | 正常 | 仍正常 ✅ |
| `notIn` 中文 | — | total=9（11 行排除 2 行）✅ |
| `in` int 列 `4,5` | 正常 | 仍正常 ✅ |
| `in` 日期列按天区间 | 正常 | 仍正常（单个/两个日期各 1/2 行）✅ |
| 值含逗号 `北京市,新疆` | total=0（被拆成两个值） | `Values` 数组 total=1 ✅ |
| 老写法 `wheres` 字符串 in 中文 | total=0 | total=1 ✅（未改调用方也自动修好） |
| 新增中文行→筛选→删除 | — | ID 13 `中文筛选验证` 筛得到，删除后表回到原 11 行 ✅ |
| 导出带中文 in 条件 | — | 200，2737 字节 ✅ |
| 列去重值（中文） | 正常 | 11 个中文值正常 ✅ |

`dotnet build` 0 错误；`npm run build` ✓ 37.58s。

**顺带修好的**：所有走 `CreateExpression(values, In)` 的地方都受益——批量删除按主键 `in`、
明细表按外键 `in`、工作流条件筛选的 `in`（`WorkFlowFilter`），这些在字符串主键/外键或中文条件值时
本来有同样的隐患。

**本次为纯代码改动，无数据库变更。**

### 20. 表格快捷复制（代码生成器新增列设置） ✅ (2026-08-24 完成)

**需求来源**：参考项目 `C:\ShelfLifeMgt\ShelfLife.Web\src\extension\system\Sys_User.js` 里写了一句
`this.initCopyBtn(['NTID', 'Email'])`，效果是表格里这两列的值后面跟一个蓝色小复制图标，点一下把值复制到粘贴板
并提示`【值】已复制`。用户希望**在代码生成器里加一个列设置，勾了就有这个效果**，不用每个页面手写扩展代码。

**参考项目是怎么实现的**：扩展 js 里遍历传进来的字段名，把该列的 `column.render` 整个改写成"原值 + 一个 `<i>` 图标"，
点击时创建一个临时 `<input>`、`select()` + `document.execCommand('copy')`。

**为什么不照搬**：① 它占用了 `column.render`，而我们的列可能本来就配了 `render`/`formatter`/`bind`(字典)/`link`/日期格式化，
覆盖等于把这些显示逻辑冲掉；② 它复制的是 `row[field]` 原始值，字典列会复制出一个 key（用户看到的是"男"，复制到的是"1"），
没有意义。所以做成**框架级的列开关**：由代码生成器写进 `options.js`，`VolTable` 在只读渲染链的**最后**追加图标，
复制的是 `cellFormatter` 算出来的**显示文本**。

| 文件 | 改动 |
|---|---|
| `VOL.Entity/DomainModels/Core/Sys_TableColumn.cs` | 新增 `int? QuickCopy`（紧邻 `HeaderFilter`，同样是 0/null 关、1 开） |
| `VOL.Builder/.../Sys_TableInfoService.cs` | `GetGridColumns` 里 `if (vue)`→`if (!app)` 分支中，`HeaderFilter` 之后追加 `quickCopy:true,`。**app 端不生成**——移动端没有粘贴板图标这个交互 |
| `DB/sqlserver/升级脚本_20260824_表格快捷复制.sql` | 幂等加列 `Sys_TableColumn.QuickCopy INT NULL` |
| `vol.web/src/views/builder/builderData.jsx` | 表结构页新增"快捷复制"复选列（`renderBoolCheckboxCell` + `renderColumnHeaderTip` 表头提示），位置在"汇总"前 |
| `vol.web/src/views/builder/coderV2Table.vue` | `TAB_VISIBLE_FIELDS.advanced` 加入 `quickCopy`——不加会被 `applyTableStructureColumnVisibility()` 隐藏（列配好了但界面上找不到） |
| `vol.web/src/components/basic/VolTable/VolTableProvider.js` | 新增 `getQuickCopyText`/`hasQuickCopyText`/`quickCopyCell`：取 `cellFormatter` 的显示文本，`navigator.clipboard` 优先、退回隐藏 textarea + `execCommand`，提示 `【值】已复制`（值超 30 字符截断，避免提示框占满屏），文案走 `$ts` 支持多语言 |
| `vol.web/src/components/basic/VolTable.vue` | 只读模板末尾追加 `<i class="el-icon-document-copy quick-copy-icon">`（值为空的单元格不显示）；列的 `class-name` 改走 `getColumnClass`，勾了快捷复制的列加标记类 `quick-copy-cell` |
| `vol.web/src/components/basic/VolTable/VolTable.less` | 图标样式 + `td.quick-copy-cell .cell > div{display:inline}` |

**布局坑（本功能特有）**：字典列和自定义 `formatter` 的列，值是渲染在一个**块级 `<div>`** 里的，
直接在后面加 `<i>` 会掉到下一行、把行高撑开。所以给这些列打标记类，只在这些单元格内把 div 改成行内，
不影响其它列。

**使用步骤**：代码生成器 → 选表 → 表结构 → 高级页签勾选"快捷复制" → 保存 → **重新生成页面**
（`QuickCopy` 只在生成页面时被读取，光保存不生成没有效果，和 `EditType` 一样）。

**E2E 验证** ✅（`Ren` 表临时把 `Name`/`Sex` 勾上快捷复制、`Birth` 故意不勾作对比，
自签 JWT 调 `LoadTableInfo`/`Save`/`createVuePage`，playwright-core + 本机 msedge 打开 `/#/Ren`）：

| 场景 | 结果 |
|---|---|
| 生成的 `options.js` | 只有 `Name`/`Sex` 带 `quickCopy:true`，`link`/`filterData`/`edit` 等原有配置未被破坏 ✅ |
| 表格图标数量 | 60 个（30 行 × 2 列）✅ |
| 点击复制（中文值） | 提示 `【广东省】已复制`，读粘贴板 = `广东省`（中文往返正确）✅ |
| 未勾选的 `Birth` 列 | 0 个图标 ✅ |
| 控制台错误 | 只有两个已知未修问题（应用列表 401、`http.js:130`），无新增 ✅ |
| 编译 | `dotnet build` 0 错误；`npm run build` ✓ 25.21s ✅ |

**未验证**：① app（移动端）——按设计根本不生成该属性；② `VolTable/VolTable-V2-Render.jsx`（虚拟表格的
另一套渲染器）**未接线**，全仓库没有任何地方 import 它，等哪天启用虚拟表格再补；③ mysql/pgsql 的基础建表脚本
**未加该列**（与 `Sys_UserShortcut`/`Sys_DbConnection` 一致，自研功能只出 sqlserver 升级脚本）。

**数据库变更**：`升级脚本_20260824_表格快捷复制.sql`，**开发库 vol_v3 已执行**，STG/PRD 未执行。

**验证后收尾**：`Ren` 表四列的 `QuickCopy` 已全部改回 null 并重新生成页面，
`views/sys/ren/Ren/options.js` 已回到不含 `quickCopy` 的原内容；临时脚本目录 `.tmpwork`（含自签 token）已删除。

---

## 三、验证结果汇总

| 验证项 | 方式 | 结果 |
|---|---|---|
| 后端编译 | `dotnet build VOL.sln` | ✅ 0错误 |
| 前端构建 | `npm run build` (vite) | ✅ 成功 |
| 后端启动 | 9991端口 Development | ✅ 正常监听 |
| 字典接口 | GetVueDictionary(dbServer) | ✅ 返回字典项(中文正常) |
| **多库切换E2E** | TestDB指向master库+DB_NAME() | ✅ 返回"master"，真实切库 |
| 多角色查询 | getUserRoles | ✅ 角色树+已有角色 |
| 多角色保存 | saveRole 差量无损测试 | ✅ 成功 |
| 用户列表 | getPageData RoleName | ✅ 显示全部角色 |
| **消息入库E2E** | Hub发送(离线收件人) | ✅ 入库成功，提示登录后可查看 |
| **在线实时推送** | 双SignalR连接实测 | ✅ 在线收件人实时收到 |
| 已读未读流转 | 未读1→标记已读→0；全部已读 | ✅ 成功 |
| 发送记录统计 | GetSentMessages/GetRecipientStatus | ✅ 已读未读人数、阅读时间正确 |
| cron中文描述 | API新建任务(0 30 8 * * ?) | ✅ 自动生成"每天08:30执行" |
| **多语言生成语言包** | GET createLanguagePack(自签JWT) | ✅ "语言包生成成功,共290条翻译数据" |
| 语言包静态访问 | GET /lang/en.js、/lang/zh-tw.js | ✅ 200 纯JSON |
| 语言设置页数据 | POST getPageData | ✅ 290行正常分页 |
| **免验证码登录** | POST login 不带验证码字段 | ✅ 错误密码报"用户名或密码错误"(非验证码错误)，正确密码登录成功返回token |
| 语言包CORS预检 | OPTIONS /lang/en.js 带Origin+Authorization | ✅ 204 + Access-Control-Allow-Origin/Headers |
| 语言包CORS响应 | GET /lang/en.js 带Origin | ✅ 200 + Access-Control-Allow-Origin |
| **实体级分库-查询** | getPageData/Async、getDetailPage(ShardDb→tempdb) | ✅ 数据来自 tempdb |
| **实体级分库-写入** | add/addAsync/update(改+增+删明细)/del/delAsync | ✅ 全部落 tempdb，级联删除 0 孤儿 |
| **无泄漏到默认库** | vol_v3 同名三张表 MAX(CreateDate) | ✅ 仍是 2025-03-19/2023-05-19 未被触碰 |
| **跨库双提交** | 业务写 tempdb 时的 Sys_Log | ✅ Sys_Log 仍写默认库 vol_v3 |
| **跨库事务回滚** | 明细数值溢出强制失败 | ✅ 主表停在 V2，原明细完好 |
| **流水号按业务库** | IdentityCode.Create | ✅ TS202608210001~0003(默认库同名表未动) |
| **主子表跨库拦截** | 去掉明细 DBServer | ✅ 运行时抛预期异常 + 生成代码时给提示 |
| **历史 DBServer 兼容** | MES_Customer(ServiceDbContext 未注册) | ✅ 从默认库返回 5 行 |
| 表头筛选(分库表) | getColumnDistinctValues | ✅ 去重值来自业务库 |
| 导出(分库表) | export | ✅ 200，3029 字节 |
| 字典多库三接口 | GetVueDictionary/GetTableDictionary/GetSearchDictionary | ✅ 中文数据正确(修复了 in @data 语法错误) |
| **连接串不可见字符** | User ID 中混入 U+00A0/全角空格/零宽空格/换行 | ✅ 改前全报"不支持的关键字:user id"，改后四种全部连接成功 |
| 证书错误给建议 | TestConnection(不带 TrustServerCertificate) | ✅ 追加"【建议】…加上 TrustServerCertificate=True;" |
| 数据库类型选错给建议 | TestConnection(Kdbndp/PgSql/Oracle/MySql + SqlServer 连接串) | ✅ 四种均追加"请改选SqlServer" |
| 非法数据库类型拦截 | TestConnection(dbType=Foo) | ✅ "不支持的数据库类型Foo,请从下拉中选择"(改前静默回退) |
| **业务表叫 Sys_xxx 分库** | Sys_Area/getPageData(shelflife 库) | ✅ total=11 中文完好(改前 Invalid object name 'Sys_Area') |
| 业务表 Sys_xxx 增删改 | add→update→del(ID 12) | ✅ 全部落 shelflife，删完回到原 11 行 |
| 业务表 Sys_xxx 筛选/导出 | getColumnDistinctValues / export | ✅ 5 个去重值；导出 200，2983 字节 |
| 框架表未被误路由 | Sys_User/Sys_Dictionary/Sys_Role/Sys_Log/Menu | ✅ 全部仍从默认库返回(2/35/3/1070/95) |
| 框架表名重名拦截 | 代码生成器对 Sys_Log 生成 | ✅ "与框架自带的表重名…请把业务表改个名字再生成" |
| **表头筛选中文 in** | Sys_Area/Sys_Dictionary/Sys_Menu/Sys_User 勾选中文值 | ✅ 1/2/2/1 行（改前全部 total=0） |
| 表头筛选英文数字 in | admin / 数字 8 / int 列 4,5 | ✅ 仍正常（本来就正常，确认未回归） |
| 表头筛选中文 notIn | Sys_Area notIn 两个中文 | ✅ total=9（11 行排除 2 行） |
| 表头筛选日期列 in | Sys_Dictionary 按天区间(1个/2个日期) | ✅ 1/2 行，按天匹配未受影响 |
| **筛选值含逗号** | `Values:["北京市,新疆"]` | ✅ total=1（改前按 Value 拆逗号 total=0） |
| 老写法兼容 | `wheres` 字符串 in 中文 / 逗号拼接 Value | ✅ 1/2 行（未改调用方也自动修好） |
| 中文筛选闭环 | 新增中文行 ID 13→按中文筛出→删除 | ✅ 筛得到，删除后回到原 11 行(未改用户数据) |
| 带中文条件导出 | export + 中文 in 条件 | ✅ 200，2737 字节 |
| **快捷复制生成属性** | Ren 表勾 Name/Sex 后 createVuePage | ✅ options.js 仅这两列有 `quickCopy:true`，原有配置未破坏 |
| 快捷复制图标渲染 | 浏览器 `/#/Ren` 表格 | ✅ 60 个图标(30行×2列)；未勾的 Birth 列 0 个 |
| 快捷复制点击(中文) | 点 Name 列图标 + 读粘贴板 | ✅ 提示`【广东省】已复制`，粘贴板 = `广东省` |

测试方法：appsettings 的 JWT Secret 自签 HS256 token 直接调接口（无需验证码登录）。

---

## 四、数据库变更记录（开发库 vol_v3 已执行）

1. `升级脚本_20260730_多数据库支持.sql`: DBServer列（已存在跳过）+ 字典 `dbServer`
2. `升级脚本_20260731_多角色支持.sql`: Sys_UserRole 表 + 现有用户主角色初始化（2行）
3. `升级脚本_20260731_站内消息.sql`: Sys_Message + Sys_MessageUser 表（含索引）
4. `升级脚本_20260731_定时任务频率描述.sql`: Sys_QuartzOptions 增加 CronDescr/CronStr 列
5. `升级脚本_20260731_多应用支持.sql`: Sys_Application 表+种子、Sys_Role.AppID 列、字典 application、"应用管理"菜单
6. `升级脚本_20260804_多语言支持.sql`: Sys_Language 表 + "语言设置"菜单(系统管理下) + 种子290条中英翻译
7. `升级脚本_20260810_代码生成器编辑模式.sql`: Sys_TableInfo.EditType 列
8. `升级脚本_20260820_系统配置表.sql`: Sys_ConfigSetting 表 + 生成路径配置项
9. `升级脚本_20260820_首页快捷导航.sql`: Sys_UserShortcut 表(UserId+AppId+MenuId 唯一索引)
10. `升级脚本_20260821_多数据库管理.sql`: Sys_DbConnection 表 + "数据库管理"菜单(Auth 不含 Delete)
11. `升级脚本_20260824_表格快捷复制.sql`: Sys_TableColumn.QuickCopy 列（0/null 关、1 开）

**实体级分库路由(功能 16)为纯代码改动，无数据库脚本**——实体的 `DBServer` 由代码生成器写进 `[Entity]` 特性。
**连接报错自查性改善(功能 17)同为纯代码改动，无数据库脚本。**
**业务表名 `Sys_xxx` 分库修复(功能 18)同为纯代码改动，无数据库脚本**——涉及的 `Sys_Area` 表与
`shelflife` 连接均由用户在界面上自行创建。
**表头筛选中文修复(功能 19)同为纯代码改动，无数据库脚本**——**特别说明：不需要、也不应该去改数据库排序规则**，
问题在于值以字面量而非参数送进数据库，改排序规则会影响全库所有字符串比较与索引。
**表格快捷复制(功能 20)只加一列**（`Sys_TableColumn.QuickCopy`），mysql/pgsql 的基础建表脚本按既有做法未同步
（自研功能一律只出 sqlserver 升级脚本）。

**同步到 STG/PRD 时**: 脚本均幂等，按顺序执行即可。中文乱码注意：
sqlcmd 执行含中文的脚本请用 SSMS 执行，或将脚本另存为 UTF-16 编码后再用 sqlcmd。

---

## 五、风险提示与注意事项

1. **多角色生效需重新登录/清缓存**: 设置角色后已自动清该用户缓存，用户下次请求即生效
2. **未注册的 DBServer 名称自动回退默认库**: 兼容历史数据（如实体特性中的 "SysDbContext"）；
   注意各层归一化规则必须一致(运行时/代码生成器/方言选择)，否则会出现"这边当默认库、那边当另一个库"的错配
3. **跨库不支持分布式事务**: 主子表**必须配在同一个库**(框架会在运行时和生成代码时各拦一次)；
   业务库与默认库(日志/权限)之间是链式本地事务，业务库先提交——极端情况下可能出现业务已提交而日志未落库
4. **框架 `Sys_*` 表强制留在默认库**: 不接受实体特性上的 DBServer。判断依据是
   `EntityDbRouter._frameworkTables` **写死的表名单**，不是 `Sys_` 前缀——业务表完全可以叫 `Sys_xxx`
   并放在别的库（代码生成器把业务实体也生成到 `VOL.Entity` 程序集里，按前缀判断会误伤，见功能 18）。
   真正与框架表重名时，代码生成器会在生成阶段直接拦掉
5. **数据库连接只增不删、且不可改名**: 连接名 = ConfigId，被实体特性/字典/代码生成器引用；停用走 `Enabled=0`
6. **审批流写回分库实体未实测**: 需要在分库表上加 `AuditStatus` 列并配流程才能验证(涉及改库，需先经用户同意)
7. **SqlServer 连接串必须带 `TrustServerCertificate=True`**: 公司这台 SqlServer 是自签证书，
   而 `Microsoft.Data.SqlClient` 4.0+ 默认 `Encrypt=True` 并校验证书；界面上新增连接时 [数据库类型] 必须选 SqlServer，
   选错会由另一种驱动去解析连接串，报的是"不支持的关键字"这类完全指不到病根的错(现已自动追加建议，见功能 17)
8. **字符串列的 `in` 必须保持参数化**: `LambdaExtensions.GetStringInExpression` 把 in 拆成等值 or 链，
   为的是让值作为 `@参数`(nvarchar) 传给数据库。若以后有人"优化"回集合 `Contains`，在非 Unicode 排序规则的库
   （本项目就是 `SQL_Latin1_General_CP1_CI_AS`）上中文条件会立刻全部失效且不报错（见功能 19）。
   同理，前端提交 in 条件时要带 `Values` 数组，只靠逗号拼接的 `Value` 会拆坏含逗号的值
9. 演示环境 GlobalFilter 当前为关闭状态（appsettings.json Enable=false）
10. **代码生成器的列开关改完必须重新生成页面**：`QuickCopy`(快捷复制) 和 `EditType`(编辑模式) 都只在
    生成页面时被读取，光在生成器里保存不会影响已生成的页面；排查"勾了没效果"先看页面 `options.js` 里有没有该属性

---

## 六、当前运行状态

- 后端当前**未运行**（2026-08-24 功能 20 验证完成后已释放9991端口，避免与run.bat启动的实例冲突），用根目录 `run.bat` 启动
- 前端 dist 已构建；开发调试用 `npm run dev`（9990端口）
- 2026-08-05 改动生效需：重启后端 + 重新登录一次（清除残留应用视角、走新登录页）
- **验证后的收尾**：三个实体上的测试用 `DBServer="ShardDb"` 已还原（`MES_Bom_Main`/`MES_Bom_Detail` 回到
  `ServiceDbContext` 历史值即默认库，`TestService` 回到不配置）；`appsettings.json` 的 `Connections.ShardDb`
  已注释（需要回归验证分库时取消注释即可）。功能 18 的验证用的是**用户自己建的** `shelflife` 连接与
  `Sys_Area` 表，无需还原（验证中新增的 ID 12 一行已删除，表回到原 11 行）。功能 19 的验证同样用 `Sys_Area`，
  新增的 ID 13 `中文筛选验证` 已删除，表回到原 11 行；`ShowSqlLog=1` 只是启动时的环境变量，未落到配置文件。
  功能 20 在 `Ren` 表(Table_Id 89)上临时勾的快捷复制已全部改回 null 并重新生成页面，
  `views/sys/ren/Ren/options.js` 已回到不含 `quickCopy` 的原内容；临时脚本目录 `.tmpwork`（含自签 JWT）已删除

**报告更新完毕。功能 1~20 项均已完成并验证（审批流写回分库实体一项待改库后补测）。**
